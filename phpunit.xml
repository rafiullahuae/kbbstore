<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true">
    <testsuites>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include><directory>app</directory></include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <!--
            Only a fallback. tests/bootstrap.php runs after PHPUnit has applied
            this block and replaces it with a file of this process's own, so a
            suite run cannot wipe the database .env points the developer's app
            at, and two runs in one worktree cannot wipe each other's. Set
            KBB_TEST_DB to pin a specific file instead.
        -->
        <env name="DB_DATABASE" value="database/testing.sqlite"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
    </php>
</phpunit>
